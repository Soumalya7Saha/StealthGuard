package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Geometric Balance Palette
val CalcBlack = Color(0xFF1C1B1F) // Deep Geometric M3 Surface
val CalcSurfaceDark = Color(0xFF1C1B1F)
val CalcCardDark = Color(0xFF2B2930) // Geometric Surface Container High
val CalcSurfaceVariant = Color(0xFF49454F)

// Keypad Colors
val CalcNumKeyBg = Color(0xFF2B2930)
val CalcNumKeyText = Color(0xFFE6E1E5)
val CalcNumKeyPressed = Color(0xFF3B3842)

val CalcFuncKeyBg = Color(0xFF49454F)
val CalcFuncKeyText = Color(0xFFE6E1E5)
val CalcFuncKeyPressed = Color(0xFF5A5661)

// Operator Keys (M3 Primary Lavender)
val CalcOpKeyBg = Color(0xFFD0BCFF)
val CalcOpKeyText = Color(0xFF381E72)
val CalcOpKeyActiveBg = Color(0xFFEADDFF)
val CalcOpKeyActiveText = Color(0xFF21005D)

// Equals Key (M3 Primary Container Lavender Accent)
val CalcEqualsBg = Color(0xFFEADDFF)
val CalcEqualsText = Color(0xFF21005D)

// Covert & Security Accents
val StealthShieldBlue = Color(0xFFD0BCFF)
val StealthAlertRed = Color(0xFFF2B8B5)
val StealthSuccessGreen = Color(0xFFA8DAB5)
val StealthWarningAmber = Color(0xFFFFD8E4)

val TextPrimary = Color(0xFFE6E1E5)
val TextSecondary = Color(0xFF938F99)
val TextTertiary = Color(0xFF79747E)
val DividerDark = Color(0xFF49454F)
val SubtlePillIndicator = Color(0x1AFFFFFF)
